# 🍱 주간 식단 자동 반영 플레이북 (Slack → Claude → Vercel)

이 문서는 **#5_공유해요_지하식당 채널(`C05B4T302KS`)에 주간 식단표 이미지가 올라오면,
자동으로 실행된 Claude Code 세션이 사이트(`at2eat-d2sf.vercel.app`)를 업데이트**하기 위한 표준 절차입니다.

> ⚠️ **무인 자동화 + main 직접 푸시** 설정입니다. 검토(PR) 단계가 없으므로,
> 이 문서의 절차를 **그대로** 따르고, 푸시 전 반드시 `npm run validate` 와 `npm run build` 를 통과시키세요.

---

## 0. 전체 흐름

```
[Slack 채널에 식단표 이미지 업로드]
        │  (Slack 트리거)
        ▼
[Claude Code on the web 세션 자동 시작 — 이미지 + 고정 프롬프트 전달]
        │
        ▼
[이미지 판독 → src/data/menu-YYYY-wN.js 생성 → index.js 등록]
        │
        ▼
[npm run validate  &&  npm run build  로 검증]
        │
        ▼
[main 에 커밋·푸시 → Vercel 자동 재배포]
```

배포 인프라는 이미 구성되어 있습니다(`vercel.json`, main 푸시 시 자동 재배포).
**세션이 해야 할 일은 "이미지를 정확한 데이터 파일로 변환 → 검증 → main 푸시" 뿐입니다.**

---

## 1. Slack 트리거 설정 (플랫폼 측 · 최초 1회)

이 부분은 레포가 아니라 **Claude Code on the web 플랫폼**에서 설정합니다.
(이 세션 자체도 Slack 에서 시작되었습니다.) 설정 방법은 공식 문서를 참고하세요:
<https://code.claude.com/docs/en/claude-code-on-the-web>

트리거를 만들 때 세션에 전달할 **고정 프롬프트**로 아래를 권장합니다:

> 첨부된 지하식당 주간 식단표 이미지를 읽고 `MENU_INGESTION.md` 플레이북에 따라
> 새 주차 데이터 파일을 만들어 `src/data/index.js` 에 등록한 뒤,
> `npm run validate` 와 `npm run build` 를 통과시키고 `main` 브랜치에 커밋·푸시해 줘.

- **대상 레포**: `sykang-max/d2sf-cafeteria-menu`
- **첨부**: 식단표 이미지(들). 조식/중식/석식/테이크아웃이 여러 장이면 모두 첨부.
- 트리거가 main 에 직접 푸시할 수 있도록 권한을 부여하세요.

---

## 2. 세션 실행 절차 (Claude 가 따르는 단계)

### 단계 1 — 이미지 판독
첨부된 식단표 이미지를 읽고 다음을 추출합니다.
- 적용 주(날짜 범위, 요일별 날짜)
- 식사 구분: `조식` `중식` `석식` `테이크아웃`
- 코너명(예: `Korean A`, `International B`, `Snack`, `백반`, `라면코너` …), 단가(원)
- 코너·요일별 메뉴 구성 항목
- 특별 태그(단가/건강/Welgreen/월드컵/맛집/수산물 등) 표기

> 글자가 흐릿해 확신이 안 서는 항목은 **임의로 지어내지 말고**, 가장 그럴듯한 판독값을 쓰되
> 커밋 메시지에 "판독 불확실: …" 로 남겨 추후 사람이 확인할 수 있게 합니다.

### 단계 2 — 데이터 파일 생성
`src/data/menu-2026-w2.js` 를 **복사**해 새 파일 `src/data/menu-YYYY-wN.js` 를 만듭니다.
파일명·`id` 규칙은 아래 [4. 주차 번호 규칙](#4-주차-번호-규칙) 참고.

스키마(절대 변경 금지):

```js
// S(식사, 요일, 코너, 단가, 유형, [[메뉴명, kcal], ...], 태그?, 사이드여부?)
const S = (meal, day, corner, price, cuisine, items, tag = null, side = false) =>
  ({ meal, day, corner, price, cuisine, items, tag, side });
```

| 필드 | 허용 값 |
|------|---------|
| `meal` | `조식` `중식` `석식` `테이크아웃` |
| `day` | `월` `화` `수` `목` `금` (테이크아웃 상시 메뉴는 요일 회전) |
| `cuisine` (유형) | `한식` `중식` `일식` `양식` `분식` `기타` |
| `tag` | `null` 또는 `단가` `건강` `welgreen` `월드컵` `맛집` `수산물` |
| `price` | 숫자(원). 추가배식대 등 무료는 `0` + `side=true` |
| `items` | `[["메뉴명", kcal추정치], ...]` |

- **유형/태그 값은 `src/theme.js` 의 `CUISINE`·`TAG` 키와 정확히 일치해야** 합니다.
  화면에 새 태그가 필요하면 **먼저 `src/theme.js` 의 `TAG` 에 추가**한 뒤 사용하세요.
- 테이크아웃 상시 메뉴는 `staticTakeout` 배열: `["메뉴명","유형",kcal,단가]`.

`week` 객체 마무리:

```js
export const week = {
  id: "2026-w3",                          // 고유 id (YYYY-wN)
  label: "6월 3주차",                      // 드롭다운 표시명
  range: "2026.06.15(월) ~ 06.19(금)",     // 상단 날짜 범위
  days: [["월","06.15"],["화","06.16"],["수","06.17"],["목","06.18"],["금","06.19"]],
  sets,
  staticTakeout,
};
```

### 단계 3 — 주차 목록 등록
`src/data/index.js` 에서 새 파일을 import 하고 `weeks` 배열 **맨 앞**에 추가합니다.
(`weeks[0]` 이 사이트 기본 선택 주차)

```js
import { week as w2026w3 } from "./menu-2026-w3.js";
import { week as w2026w2 } from "./menu-2026-w2.js";

export const weeks = [w2026w3, w2026w2]; // 최신 주차를 맨 앞에
```

### 단계 4 — 검증 (푸시 전 필수 게이트)

```bash
npm run validate   # 데이터 구조·유형·태그·칼로리 범위 검사
npm run build      # 실제 빌드로 import 오류까지 확인
```

`validate` 가 **❌ 오류**를 내면 푸시하지 말고 데이터를 고친 뒤 다시 실행합니다.
⚠️ 경고는 사람이 확인할 사항이며 푸시를 막지는 않습니다.

### 단계 5 — 커밋 & main 푸시 (즉시 배포)

```bash
git add src/data/menu-YYYY-wN.js src/data/index.js
git commit -m "feat(menu): YYYY N월 N주차 식단 추가"
git push origin main
```

푸시 후 Vercel 이 자동 재배포합니다. 1~2분 뒤 `at2eat-d2sf.vercel.app` 에서 확인하세요.

---

## 3. 칼로리(kcal) 추정 규칙
이미지에는 보통 칼로리가 없으므로 **1인분 기준 합리적 추정치**를 넣습니다.
기존 `menu-2026-w2.js` 의 유사 메뉴 값을 참고해 일관되게 부여하세요(예: 밥류 300~310,
국 70~130, 김치류 15~20, 메인 단백질 300~500, 라면 500~520). 항목당 0~2000 범위를
벗어나면 `validate` 가 경고합니다.

## 4. 주차 번호 규칙
- 기본: **직전 최신 주차(`weeks[0]`)의 번호에서 +1**, 라벨·날짜는 **이미지에 적힌 범위 그대로**.
- 라벨은 "N월 N주차" 형식. 월의 첫 월요일이 포함된 주를 그 달 1주차로 봅니다.
  (예: 2026-06 첫 월요일 6/1 → 6/1주가 1주차, 6/8~ 2주차, 6/15~ 3주차)
- `id` 는 `YYYY-wN` (전역 고유, 중복 금지). 월 경계로 주차가 헷갈리면 이미지 날짜를 우선합니다.

## 5. 주의 / 하지 말 것
- 디자인·레이아웃·컴포넌트 코드는 **건드리지 않습니다**. 데이터 파일과 `index.js`, (필요 시) `theme.js` 의 `TAG` 만 수정.
- 스키마(`S` 헬퍼 시그니처, `week` 필드명)를 바꾸지 않습니다.
- 판독이 불확실해도 빈 항목/가짜 메뉴로 채우지 말고 불확실 표시를 남깁니다.
- `validate` 또는 `build` 실패 상태로 main 에 푸시하지 않습니다.
